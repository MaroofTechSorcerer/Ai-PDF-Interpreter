# PDF Interpreter Backend

This is the FastAPI backend for the PDF Interpreter app. It provides advanced PDF features powered by AI.

## Setup

```bash
pip install -r requirements.txt
uvicorn main:app --reload
``` 